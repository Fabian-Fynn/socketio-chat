{
  "name": "socket.io-chat",
  "version": "0.0.3",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "copy": "mkdir ./dist && cp package.json ./dist/package.json && cp app/*.js ./dist && cp app/*.html ./dist",
    "build": "npm run clean && npm run copy"
    },
  "dependencies": {
    "express": "^4.10.2",
    "jsdom": "^4.0.4",
    "socket.io": "^1.3.5",
    "xmlhttprequest": "^1.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fabian-fynn/socketio-chat.git"
  }
}
